# Game Store System - 遊戲商城系統

一個整合遊戲大廳（Lobby）與遊戲商城（Store）的網路平台。

## 📁 專案結構

```
netgame-hw3/
├── server/                    # 伺服器端
│   ├── server_main.py        # 主程式
│   ├── utils.py              # 通訊協定工具
│   ├── database.json         # 資料庫
│   └── storage/              # 上架遊戲存放區
├── developer_client/          # 開發者客戶端
│   ├── dev_client.py         # 主程式
│   ├── games/                # 本地遊戲開發區
│   │   ├── gomoku/          # 五子棋遊戲
│   │   └── guess_number/    # 猜數字遊戲
│   └── template/             # 遊戲範本
├── player_client/             # 玩家客戶端
│   ├── lobby_client.py       # 主程式
│   └── downloads/            # 下載的遊戲
│       ├── player1/
│       ├── player2/
│       └── ...
├── makefile                   # 快速啟動腳本
├── requirements.txt           # Python 依賴
└── readme.md                  # 本檔案
```

## 🚀 快速開始

### 環境需求

- Python 3.8+
- tkinter (GUI 遊戲需要，通常已內建)

### 安裝依賴

```bash
pip install -r requirements.txt
```

### 啟動方式

#### 方式一：使用 PowerShell (Windows 推薦)

```powershell
# 啟動伺服器
.\run.ps1 server

# 啟動開發者客戶端
.\run.ps1 dev

# 啟動玩家客戶端
.\run.ps1 player
```

#### 方式二：使用 Makefile (Linux/Mac)

```bash
# 啟動伺服器
make server

# 啟動開發者客戶端
make dev

# 啟動玩家客戶端
make player
```

#### 方式二：直接執行

**1. 啟動伺服器**
```bash
cd server
python server_main.py
```
伺服器預設監聽 `0.0.0.0:5000`

**2. 啟動開發者客戶端**
```bash
cd developer_client
python dev_client.py [SERVER_IP] [SERVER_PORT]
```
例如：`python dev_client.py 127.0.0.1 5000`

**3. 啟動玩家客戶端**
```bash
cd player_client
python lobby_client.py [SERVER_IP] [SERVER_PORT]
```
例如：`python lobby_client.py 127.0.0.1 5000`

## 👨‍💻 測試帳號

### 開發者帳號
| 帳號 | 密碼 |
|------|------|
| dev1 | dev123 |
| dev2 | dev456 |

### 玩家帳號
| 帳號 | 密碼 |
|------|------|
| player1 | pass123 |
| player2 | pass456 |
| player3 | pass789 |

也可以自行註冊新帳號。

## 📖 使用流程

### 開發者流程

1. 啟動 Developer Client
2. 登入開發者帳號
3. 選擇「上架新遊戲」
4. 選擇本地遊戲目錄（需包含 `config.json`）
5. 確認上架資訊後上傳

### 玩家流程

1. 啟動 Lobby Client
2. 登入玩家帳號
3. 進入「遊戲商城」瀏覽遊戲
4. 選擇遊戲並下載
5. 進入「遊戲房間」建立或加入房間
6. 人數足夠後由房主開始遊戲
7. 遊戲結束後可撰寫評論

## 🎮 內建示範遊戲

### 五子棋（Gomoku）
- 類型：雙人 GUI 遊戲
- 人數：2 人
- 說明：經典五子棋，先連成五子者獲勝

### 猜數字大戰（Guess Number）
- 類型：多人 GUI 遊戲  
- 人數：2-6 人
- 說明：猜 1-100 的隱藏數字，猜中者獲勝

## 🛠️ 遊戲開發指南

### 遊戲規格

每個遊戲目錄需包含 `config.json`：

```json
{
    "name": "遊戲名稱",
    "description": "遊戲簡介",
    "version": "1.0.0",
    "game_type": "GUI",
    "min_players": 2,
    "max_players": 2,
    "server_command": ["python", "server.py"],
    "client_command": ["python", "client.py"]
}
```

### 命令列參數

遊戲 Server 端會收到：
- `--port`: 分配的連線埠

遊戲 Client 端會收到：
- `--host`: 伺服器 IP
- `--port`: 連線埠

## 📝 功能清單

### 開發者功能 (Developer)
- [x] D1: 上架新遊戲
- [x] D2: 更新遊戲版本
- [x] D3: 下架遊戲
- [x] 查看我的遊戲列表

### 玩家功能 (Player)
- [x] P1: 瀏覽遊戲商城
- [x] P2: 下載/更新遊戲
- [x] P3: 建立/加入房間、啟動遊戲
- [x] P4: 評分與評論
- [x] 查看大廳狀態
- [x] 查看已下載遊戲

## ⚙️ 系統架構

```
┌─────────────────┐     ┌─────────────────┐
│ Developer Client │     │  Lobby Client   │
│                 │     │   (Player)      │
└────────┬────────┘     └────────┬────────┘
         │                       │
         │      TCP/JSON         │
         └───────────┬───────────┘
                     │
              ┌──────▼──────┐
              │   Server    │
              │ (Main Hub)  │
              ├─────────────┤
              │ - 帳號管理   │
              │ - 遊戲管理   │
              │ - 房間管理   │
              │ - 評論系統   │
              └──────┬──────┘
                     │
              ┌──────▼──────┐
              │  Database   │
              │ (JSON File) │
              └─────────────┘
```

## 📡 通訊協定

使用 TCP Socket + JSON 格式：

```
[Header: 4 bytes (資料長度, Big Endian)] + [Body: JSON UTF-8]
```

## 🔧 故障排除

### 連線失敗
1. 確認伺服器已啟動
2. 確認 IP 和 Port 正確
3. 檢查防火牆設定

### 遊戲無法啟動
1. 確認已下載遊戲
2. 確認遊戲版本與伺服器一致
3. 檢查 `config.json` 格式

### GUI 遊戲問題
1. 確認已安裝 tkinter
2. Windows: 通常已內建
3. Linux: `sudo apt install python3-tk`

## 📄 License

MIT License
